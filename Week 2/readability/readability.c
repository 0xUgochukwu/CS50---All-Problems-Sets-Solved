#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{

}

int count_letters(string text)
{
    int letters = 0;
    for (int i = 0, n = strlen(text); i < n; i++)
    {
        if
    }
}